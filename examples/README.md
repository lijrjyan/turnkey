# Examples

Examples are source templates, not checked-in run results. Generate a fresh four-file bundle with the current Turnkey version whenever you need evidence.

- `external_detector/length_detector.py`: the smallest complete file entrypoint for `turnkey dev`.

```bash
uv run turnkey dev examples/external_detector/length_detector.py:build
```
